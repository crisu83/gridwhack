package gridwhack.grid;

public class GridWhackGrid extends Grid
{
	/**
	 * Creates the grid.
	 * @param widthInCells the width of the grid in cells.
	 * @param heightInCells the height of the grid in cells.
	 */
	public GridWhackGrid(int widthInCells, int heightInCells)
	{
		super(widthInCells, heightInCells);
	}
}
